# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

.env

logs/

# Claude Code — local machine overrides (do NOT commit machine-specific paths/allows)
.claude/settings.local.json
# Keep versioned: .claude/settings.json and .mcp.json (tracked intentionally)